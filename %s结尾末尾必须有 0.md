# 一、`\0` 到底是什么

`\0` 是**转义字符**，本质是一个字节、数值等于 `0`，学名**字符串结束标志**。

C 语言没有专门记录字符串 “长度” 的变量，`printf`、`strlen` 这些函数靠**读到 `\0` 就停止**，判断字符串到头了。

## 1. 带 `\0`（合法字符串）

c

```
char str[] = "abc";
```

内存真实存储（自动补 `\0`）：

`'a'` → `'b'` → `'c'` → `\0`

- `printf("%s", str)`：读到 `\0` 立刻停止，只输出 `abc`。
- `strlen(str)`：计数到 `\0` 为止，结果为 3，不计结束符。

## 2. 不带 `\0`（只是普通字符数组，不是字符串）

c

```
char arr[] = {'a','b','c'};
```

内存只有：`'a'` → `'b'` → `'c'`，没有结束标记。

当你用 `%s` 打印时：

函数不知道哪里是结尾，会**持续向后读内存**，读取后面随机垃圾数据，出现乱码，严重时程序崩溃。

# 二、两种定义方式对比

### 方式 1：双引号字面量（自动追加 `\0`，推荐）

c

```
char s1[] = "hi";
// 内存：h i \0  总长度3字节，是标准字符串
```

### 方式 2：大括号逐个赋值（必须手动加 `\0`）

c

```
char s2[] = {'h','i','\0'}; // ✅ 合法字符串
char s3[] = {'h','i'};     // ❌ 无\0，不能当字符串用
```

# 三、直观错误演示

c

```
#include <stdio.h>
int main()
{
    // 没有\0，不是字符串
    char bad[] = {'1','2','3'};
    // 打印会乱码，因为找不到结束标记
    printf("%s", bad);
    return 0;
}
```

运行结果：`123烫烫烫烫` 这类无意义乱码，就是越界读取内存导致。

# 四、核心一句话总结

`\0` 是字符串的**终点路标**：

1. 没有路标：程序不知道字符串在哪结束，乱读内存；
2. 有 `\0`：标准字符串，`%s`、`strlen`、`strcpy` 等函数才能正常工作。