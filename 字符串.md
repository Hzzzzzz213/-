
 
c
  

char *s = "Hello World";
char *s2 = "Hello World";
char s3[] = "Hello World";
 
 
1.  s、s2 （字符指针）
 
-  "Hello World"  本体只有1份，存只读常量区（代码段）
-  s、s2  两个指针都指向同一个原始字符串，所以打印地址一模一样  0xfdf7c 
- 只读区内容受保护： s[0]='B'  直接崩溃报错，不能改原始备份
 
2.  s3[] （字符数组）
 
- 程序运行时：把常量区的"Hello World"完整复制一份到栈空间（s3自己的内存）
- s3是独立副本，和原常量串彻底分家，地址  0xbff03d50  和s不一样
-  s3[0]='B'  ✅随便改，只修改自己这份复制出来的副本，不动原始常量字符串
 
一句话总结
 
- 指针：共用原版（常量区，只读不能改）
- 数组：拷贝出新副本（栈内存，自由修改）
 
补充细节
 
常量字符串编译器会合并优化，所有相同 "Hello World" 只存一份，所以多个指针指向同一块地址。