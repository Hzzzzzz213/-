# C 语言完整常用数据类型对照表（含基础、扩展、嵌入式专用）

### 一、基础整型（char/short/int/long/long long）


| 原生类型                         | 位数 (32 位 MCU) | 取值范围                                       | stdint 标准类型 | STM32 简化别名 | 适用场景               |
| ---------------------------- | ------------- | ------------------------------------------ | ----------- | ---------- | ------------------ |
| char                         | 8bit          | -128 ~ 127（有符号）                            | int8_t      | s8         | 字符、小范围有符号数值        |
| signed char                  | 8bit          | -128 ~ 127                                 | int8_t      | s8         | 显式声明有符号字节          |
| unsigned char                | 8bit          | 0 ~ 255                                    | uint8_t     | u8         | 寄存器、缓存、颜色、字节数据     |
| short / signed short         | 16bit         | -32768 ~ 32767                             | int16_t     | s16        | 传感器采样值、小量程整数       |
| unsigned short               | 16bit         | 0 ~ 65535                                  | uint16_t    | u16        | ADC 数值、引脚掩码、定时器计数值 |
| int / signed int             | 32bit         | -2147483648 ~ 2147483647                   | int32_t     | s32        | 通用整型、循环计数          |
| unsigned int                 | 32bit         | 0 ~ 4294967295                             | uint32_t    | u32        | 地址、时间戳、寄存器 32 位值   |
| long / signed long           | 32bit         | -2147483648 ~ 2147483647                   | int32_t     | s32        | 兼容老代码长整数           |
| unsigned long                | 32bit         | 0 ~ 4294967295                             | uint32_t    | u32        | 外设地址、延时计数          |
| long long / signed long long | 64bit         | -9223372036854775808 ~ 9223372036854775807 | int64_t     | s64        | 高精度时间、超大数值运算       |
| unsigned long long           | 64bit         | 0 ~ 18446744073709551615                   | uint64_t    | u64        | 超大计数、64 位硬件寄存器     |

## 二、浮点型（小数）

表格

| 原生类型        | 位数        | 精度          | 取值范围                   | 适用场景                 |
| ----------- | --------- | ----------- | ---------------------- | -------------------- |
| float       | 32bit     | 6~7 位有效小数   | -3.4×10³⁸ ~ 3.4×10³⁸   | 普通小数、传感器浮点计算（MCU 常用） |
| double      | 64bit     | 15~16 位有效小数 | -1.7×10³⁰⁸ ~ 1.7×10³⁰⁸ | 高精度运算、算法拟合           |
| long double | 80/128bit | 更高精度        | 范围远大于 double           | 科学计算、高精度数学库（单片机极少用）  |

## 三、特殊空类型 & 布尔类型（C99 新增）

### 1. void 空类型

- 用途：
    
    1. `void`：函数无返回值 `void LED_Init(void);`
    2. `void*` 万能指针，可接收任意类型地址（驱动、缓存通用指针）
    

### 2. bool 布尔型（需头文件 `<stdbool.h>`）

表格

|类型|位数|取值|宏定义|
|---|---|---|---|
|bool|8bit|true(1) / false(0)|#define true 1 #define false 0|

示例：

c

```
bool flag = false;
if(flag == true){}
```

## 四、无符号固定宽度扩展（嵌入式高频）

`<stdint.h>` 额外配套**最小快速类型**，工业代码常用：

表格

|类型|含义|
|---|---|
|int_fast8_t|系统运算最快的 8 位有符号整型|
|uint_fast8_t|系统运算最快的 8 位无符号整型|
|int_least16_t|至少 16 位的有符号整型|
|uint_least32_t|至少 32 位的无符号整型|
|intmax_t|系统支持最大宽度有符号整数|
|uintptr_t|能完整存储指针地址的无符号整型（地址转数字必备）|

## 五、复合数据类型（工程高频，属于衍生类型）

不属于基础原生类型，但日常开发必用：

1. **数组**：`u8 buf[10];` 连续同类型数据缓存
2. **指针**：`u32 *p_reg;` 存储硬件寄存器地址
3. **结构体 struct**：GPIO_InitTypeDef 就是结构体，打包多个参数
4. **联合体 union**：字节拆分寄存器高低位，通信协议解析
5. **枚举 enum**：状态机、引脚编号、模式选择（替代宏，类型安全）

c

```
// 枚举示例
enum LED_STATE{LED_OFF=0,LED_ON=1};
```

## 六、补充关键知识点

1. **signed /unsigned 区别**
    
    - signed：最高位为符号位，支持正负；
    - unsigned：全部位存数值，只有正数，寄存器、硬件数据一律用无符号。
    
2. **单片机推荐规范**
    
    优先用 `uint8_t / uint16_t / uint32_t`（stdint），平台移植不会乱位数；
    
    ST 库简写 u8/u16/u32 只是 typedef 别名，底层等价。
3. **char 陷阱**
    
    不同编译器默认 char 可能是 signed/unsigned，嵌入式建议显式写`signed char`/`unsigned char`避免数值溢出 bug。
