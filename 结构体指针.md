格式：`结构体变量.成员名`

c

运行

```
s.name; s.age;
```

- 适用：**不是指针、实实在在的结构体实体**

## 二、结构体指针（指针 `ps`）两种等价写法

### 写法 1：`(*指针).成员`

`(*ps).name`

1. `*ps`：把指针解引用，变回结构体实体
2. `.` 访问成员

> ==注意：括号不能丢：`*ps.name` 错误（`.`优先级高于`*`）==

### 写法 2：`->` 箭头（指针专用，最常用）

`ps->name`

- `->` = 指针访问成员专用符号，**只给结构体指针用**
- 和 `(*ps).name` 完全等价，代码更简洁

## 一句话口诀

**变量用点 `.`，指针用箭头 `->`；

指针解引用变回变量，也能用点。**

## 对应代码

c

运行

```
//1. 变量.成员
s.name
//2. 指针两种
(*ps).name
ps->name
```